const primNum = window.prompt("Digite o primeito número:")
const segNum = window.prompt("Digite o segundo número:")

alert(
  "Resultados:\n" + 
  "\nSoma: " + (primNum + segNum) +
  "\nSubtracao: " + (primNum - segNum) +
  "\nMultiplicação: " + (primNum * segNum) +
  "\nDivisão: " + (primNum / segNum)

)

